WORK ORDER — FIX: strip whitespace from ESPN credentials + report ESPN league-list evidence

Context: confirmed crash cause = newline stored inside espnS2; undici rejects the Cookie header before ESPN is contacted. User has since reconnected with a clean paste, so requests now reach ESPN — but the league list comes back empty, while the user's ESPN account has a drafted 2026 league with rosters. This order FIXES only the whitespace bug. The empty list is REPORT-ONLY here — do not fix it in this order.

Files this order may touch — no others:
- artifacts/api-server/src/lib/sessions.ts
- artifacts/api-server/src/lib/espn.ts
- artifacts/api-server/src/routes/espn.ts (only if buildHeaders lives here instead of lib/espn.ts)

Changes:
1. In createSession (sessions.ts): strip ALL whitespace (spaces, tabs, \r, \n) from espnS2 and swid before the DB insert. Example: const clean = (v: string) => v.replace(/\s+/g, "");
2. In buildHeaders: apply the same stripping to creds.espnS2 and creds.swid when assembling the Cookie string. Do NOT delete or migrate any DB rows.
3. Nothing else. No new dependencies. No frontend or api-client changes.

Verify before asking to push (report only — fix nothing here):
- Restart the server. Using the CURRENT stored session, trigger GET /api/espn/leagues.
- Paste verbatim: every ESPN URL called during that request by fetchUserLeagues / getUserLeagueIds, and the HTTP status each returned.
- State how many leagues appear in the RAW ESPN response body for season 2026 (and the 2025 fallback if it ran). If zero, paste the first ~500 characters of that raw body verbatim.
- Paste verbatim the full getUserLeagueIds function from artifacts/api-server/src/lib/espn.ts (quote only — change nothing in it).
- If anything named here does not exist, write exactly: NOT FOUND — [item]. Never invent code, URLs, or logs.

Push protocol: after verification, ask me to confirm. Do not push until I reply "Push".

final message: only git diff --stat output